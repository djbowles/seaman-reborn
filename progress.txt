# Ralph Progress Log
Started: 2026-02-25
---

## Codebase Patterns
- Build backend is `setuptools.build_meta` (NOT `setuptools.backends._legacy`)
- pytest-asyncio uses `asyncio_mode = "auto"` in pyproject.toml
- All stub files have docstrings with "Stub - implementation in US-XXX"
- Package installed in editable mode: `pip install -e ".[dev]"`
- Quality gates: `ruff check src/ tests/` + `python -m pytest tests/ -x --tb=short` + `python -m seaman_brain --version`
- Use `datetime.UTC` not `timezone.utc` (ruff UP017 rule)
- Run ruff via `python -m ruff` (not bare `ruff`) — bare command not on PATH
- Cloud LLM providers (openai, anthropic) are in `[cloud]` optional deps: `pip install -e ".[cloud]"`
- Anthropic API separates system messages from conversation — pass via `system` kwarg, not in `messages` array
- LanceDB async: `connect_async()`, `table.search()` is a coroutine (await it), then chain `.limit(k).to_list()`
- LanceDB `list_tables()` returns a response object — access `.tables` for the list of names (replaces deprecated `table_names()`)
- LanceDB stores timestamps as ISO strings, reconstruct with `datetime.fromisoformat()`
- prompt_toolkit PromptSession must be lazy-initialized (not in __init__) — fails in non-console environments like pytest
- For local-import modules (pyttsx3, etc.), mock via `patch.dict(sys.modules, {"pkg": mock})` not `patch("module.pkg")`
- pyttsx3 engines are NOT thread-safe — use single-thread ThreadPoolExecutor; create fresh engine per operation

## 2026-02-25 - US-001
- Initialized git repo, created full project scaffold
- Files changed: .gitignore, pyproject.toml, CLAUDE.md, AGENTS.md, prd.json, progress.txt, ralph.sh, config/default.toml, config/stages/*.toml, src/seaman_brain/ (all stubs), tests/ (structure + smoke tests), data/ (.gitkeep)
- **Learnings for future iterations:**
  - setuptools.build_meta is the correct build backend for pyproject.toml
  - pytest-asyncio 1.3.0 installed, uses asyncio_mode=auto
  - ruff configured with line-length=100, rules E,F,W,I,N,UP
  - All 7 subpackages have __init__.py files
  - conftest.py provides config_dir and data_dir fixtures using tmp_path
---

## 2026-02-25 - US-002
- Implemented shared types and enums: CreatureStage (5 stages), MessageRole (3 roles), ChatMessage dataclass, MemoryRecord dataclass
- Files changed: src/seaman_brain/types.py, tests/test_types.py
- **Learnings for future iterations:**
  - Use `datetime.UTC` not `timezone.utc` — ruff UP017 enforces the newer alias
  - `ruff` is not on PATH; must use `python -m ruff check src/ tests/`
  - numpy NDArray[np.float32] works well for embedding type hints
  - 13 tests in test_types.py covering enums, dataclass creation, defaults, edge cases
---

## 2026-02-25 - US-003
- Implemented TOML config loader with Pydantic v2 models
- 10 sub-config models: LLMConfig, MemoryConfig, PersonalityConfig, CreatureConfig, AudioConfig, EnvironmentConfig, NeedsConfig, GUIConfig, APIConfig, CLIConfig
- SeamanConfig top-level aggregator
- load_config() reads default.toml, load_stage_config() reads stage overrides, load_config_with_stage() merges both
- _deep_merge helper for recursive dict merging
- Files changed: src/seaman_brain/config.py, tests/test_config.py
- **Learnings for future iterations:**
  - Pydantic v2 `model_validate()` works perfectly with tomllib output (dict)
  - `model_copy(update={...})` is the Pydantic v2 way to create modified copies (replaces deprecated `.copy()`)
  - Evolution thresholds use nested Pydantic models (EvolutionThreshold) inside a dict field
  - conftest.py `config_dir` fixture creates tmp_path/config with stages/ subdir — use it for isolated config tests
  - Empty TOML file loads fine — all Pydantic defaults kick in
  - 19 tests covering happy path, stage merging, error handling, edge cases, and real config validation
---

## 2026-02-25 - US-004
- Implemented LLMProvider Protocol in llm/base.py — runtime-checkable with async chat() and stream() methods
- chat() takes list[ChatMessage] -> str, stream() takes list[ChatMessage] -> AsyncIterator[str]
- Files changed: src/seaman_brain/llm/base.py, tests/test_llm/test_base.py
- **Learnings for future iterations:**
  - Python Protocol with `@runtime_checkable` allows isinstance() checks at runtime
  - ruff import sorting (I001) requires `from __future__` separate from stdlib separate from third-party — use `ruff --fix` to auto-sort
  - AsyncIterator from collections.abc works for typing async generators in Protocol
  - 12 tests: protocol compliance (4), edge cases (4), error handling (4)
---

## 2026-02-25 - US-005
- Implemented OllamaProvider class in llm/ollama_provider.py
- Wraps ollama.AsyncClient with LLMProvider-conforming chat() and stream() methods
- Configurable via LLMConfig (model, temperature, num_ctx/max_tokens, base_url)
- Converts ChatMessage to ollama dict format via _format_messages()
- Graceful error handling: wraps all exceptions as ConnectionError with base_url in message
- stream() skips empty/None chunks
- Files changed: src/seaman_brain/llm/ollama_provider.py, tests/test_llm/test_ollama_provider.py
- **Learnings for future iterations:**
  - ollama AsyncClient.chat() signature: model, messages (list of dicts), options (dict), stream (bool)
  - Response is ChatResponse with response.message.content (can be None)
  - Streaming returns AsyncIterator[ChatResponse] when stream=True
  - Options dict keys: "temperature", "num_ctx" (not max_tokens)
  - Mock AsyncClient.chat with AsyncMock; for streaming, return an async generator
  - ruff I001 fix: `from __future__` block, blank line, stdlib, blank line, third-party, blank line, local — use `ruff --fix`
  - 14 tests: happy path (5), edge cases (4), error handling (5 including chained exceptions)
---

## 2026-02-25 - US-006
- Implemented OpenAIProvider class in llm/openai_provider.py
- Wraps openai.AsyncOpenAI with LLMProvider-conforming chat() and stream() methods
- API key from constructor arg or OPENAI_API_KEY env var, with clear ValueError if missing
- Configurable via LLMConfig (model, temperature, max_tokens)
- Converts ChatMessage to OpenAI dict format via _format_messages()
- Graceful error handling: wraps all exceptions as ConnectionError
- stream() skips empty/None chunks and empty choices
- Files changed: src/seaman_brain/llm/openai_provider.py, tests/test_llm/test_openai_provider.py
- **Learnings for future iterations:**
  - openai AsyncOpenAI client: `client.chat.completions.create()` for both chat and streaming
  - Response is ChatCompletion with `response.choices[0].message.content`
  - Streaming uses `stream=True`, chunks have `chunk.choices[0].delta.content`
  - Must handle empty `choices` list in both chat and stream responses
  - openai>=1.50 is in `[cloud]` optional deps — install with `pip install -e ".[cloud]"`
  - Constructor api_key takes precedence over OPENAI_API_KEY env var
  - 18 tests: happy path (6), edge cases (7), error handling (5)
---

## 2026-02-25 - US-007
- Implemented AnthropicProvider class in llm/anthropic_provider.py
- Wraps anthropic AsyncAnthropic client with LLMProvider-conforming chat() and stream() methods
- Key difference from OpenAI: system messages extracted separately via `system` kwarg (not in messages array)
- Multiple system messages concatenated with newlines
- API key from constructor arg or ANTHROPIC_API_KEY env var, with clear ValueError if missing
- stream() filters for `content_block_delta` events and extracts `event.delta.text`
- chat() reads `response.content[0].text` from Message response
- Files changed: src/seaman_brain/llm/anthropic_provider.py, tests/test_llm/test_anthropic_provider.py
- **Learnings for future iterations:**
  - Anthropic Messages API separates system messages from conversation — pass via `system` kwarg, not in `messages`
  - Response is Message with `response.content` list of ContentBlock, each has `.text`
  - Streaming events have `.type` attribute; only `content_block_delta` has `.delta.text`
  - When no system messages, omit the `system` kwarg entirely (don't pass None)
  - anthropic>=0.40 is in `[cloud]` optional deps alongside openai
  - 21 tests: happy path (7), edge cases (10), error handling (5 including chained exceptions)
---

## 2026-02-25 - US-008
- Implemented create_provider() factory function in llm/factory.py
- Selects OllamaProvider, OpenAIProvider, or AnthropicProvider based on config.provider string
- Lazy imports: each provider module imported only when selected (handles missing optional deps)
- Case-insensitive provider name matching via .lower()
- Clear ValueError for unknown providers listing supported options
- Clear ImportError with install instructions for missing packages
- Files changed: src/seaman_brain/llm/factory.py, tests/test_llm/test_factory.py
- **Learnings for future iterations:**
  - Lazy imports in factory prevent ImportError when optional cloud deps aren't installed
  - To test missing imports: must pop module from sys.modules AND patch builtins.__import__
  - sys.modules caching means patch.dict("sys.modules", {"pkg": None}) alone won't block submodule imports
  - 11 tests: happy path (4), edge cases (2), error handling (5 including import errors)
---

## 2026-02-25 - US-009
- Implemented EpisodicMemory class in memory/episodic.py
- Rolling buffer using collections.deque with maxlen for automatic eviction
- Configurable max_size (default 20), validates >= 1
- Methods: add(), get_recent(n), get_all(), clear(), __len__()
- get_recent handles edge cases: n<=0 returns empty, n>len returns all
- Files changed: src/seaman_brain/memory/episodic.py, tests/test_memory/test_episodic.py
- **Learnings for future iterations:**
  - collections.deque(maxlen=N) handles overflow eviction automatically — no manual pop needed
  - ruff I001 import sorting: `from __future__` needs its own block separated by blank line from stdlib imports; use `ruff --fix` to auto-sort
  - EpisodicMemory is standalone (no async, no config dependency) — simplest module in memory subsystem
  - 20 tests: happy path (7), overflow (4), edge cases (7), error handling (2)
---

## 2026-02-25 - US-010
- Implemented EmbeddingProvider class in memory/embeddings.py
- Wraps ollama AsyncClient.embed() API for single and batch text embedding
- Configurable model via MemoryConfig.embeddings.model (default: all-minilm:l6-v2)
- embed(text) returns list[float] vector, handles empty/whitespace by returning []
- embed_batch(texts) supports mixed empty/real texts — only sends non-empty to server
- All exceptions wrapped as ConnectionError with base_url in message
- Files changed: src/seaman_brain/memory/embeddings.py, tests/test_memory/test_embeddings.py
- **Learnings for future iterations:**
  - ollama AsyncClient.embed() signature: model, input (str or list[str]) -> EmbedResponse
  - EmbedResponse.embeddings is Sequence[Sequence[float]] — always a list of lists even for single input
  - Config chain: MemoryConfig.embeddings (EmbeddingsConfig) holds model name — not LLMConfig
  - For batch with mixed empties: filter empties, send only real texts, then map results back to original indices
  - 21 tests: happy path (5), edge cases (8), error handling (5), configuration (3)
---

## 2026-02-25 - US-011
- Implemented SemanticMemory class in memory/semantic.py using LanceDB async API
- Lazy connection/table initialization via _ensure_table()
- add() stores MemoryRecord with embedding validation (empty check, dimension check)
- search() returns MemoryRecord list sorted by vector similarity, respects top_k
- count() and delete_all() for management
- PyArrow schema with text, vector, timestamp (ISO string), importance, source
- Files changed: src/seaman_brain/memory/semantic.py, tests/test_memory/test_semantic.py
- **Learnings for future iterations:**
  - LanceDB async: `connect_async()` returns AsyncConnection, `table.search()` is a coroutine
  - Async search chain: `query = await table.search(vec)` then `await query.limit(k).to_list()`
  - `list_tables()` returns ListTablesResponse object — use `.tables` for name list (deprecated `table_names()`)
  - Timestamps stored as ISO strings (pa.string), not datetime — LanceDB Arrow doesn't handle tz-aware datetimes well
  - Embeddings stored as `pa.list_(pa.float32(), dim)` — fixed-size list for vector column
  - MemoryRecord.embedding is NDArray, must `.tolist()` before storing in LanceDB
  - 16 tests: happy path (6), edge cases (7), error handling (3)
---

## 2026-02-25 - US-012
- Implemented HybridRetriever class in memory/retriever.py
- Combines semantic similarity (importance as proxy) with temporal recency scoring
- Configurable weights via MemoryConfig.similarity_weight and .recency_weight (default 0.7/0.3)
- Fetches 3x top_k candidates from semantic store, then re-ranks by weighted combination
- Handles empty store, empty query embeddings, top_k<=0, same-timestamp candidates, importance clamping
- Files changed: src/seaman_brain/memory/retriever.py, tests/test_memory/test_retriever.py
- **Learnings for future iterations:**
  - HybridRetriever uses importance field as similarity proxy (0-1 range, clamped)
  - Recency score: `1 - (age / max_age)` — newer = higher; handles zero max_age (same timestamps)
  - Over-fetch from semantic store (3x top_k, capped at 50) to give re-ranking room
  - All memory module dependencies: embeddings.py -> semantic.py -> retriever.py
  - 12 tests: happy path (4), edge cases (5), error handling (3)
---

## 2026-02-25 - US-013
- Implemented MemoryExtractor class in memory/extractor.py
- Uses LLM to extract key facts from conversation history via structured prompt
- extract() sends filtered conversation (no system messages) to LLM, parses newline-separated facts
- extract_and_store() embeds each fact and persists to SemanticMemory, with per-fact error resilience
- Configurable extraction_interval from MemoryConfig (default 5 messages)
- Message counter with should_extract()/increment_counter()/reset_counter() for interval tracking
- EXTRACTION_PROMPT focuses on: user preferences, personal info, topics, observations
- Graceful degradation: LLM errors return empty list, individual fact embed/store errors skip that fact
- Files changed: src/seaman_brain/memory/extractor.py, tests/test_memory/test_extractor.py
- **Learnings for future iterations:**
  - MemoryExtractor depends on LLMProvider, EmbeddingProvider, and SemanticMemory — three injected collaborators
  - System messages should be filtered out before sending conversation to extraction LLM
  - LLM "NONE" response is the sentinel for no-facts-found — must handle case-insensitively
  - Per-fact error handling in extract_and_store means one bad embedding doesn't block other facts
  - Memory module dependency chain updated: embeddings.py -> semantic.py -> retriever.py, extractor.py (parallel leaf)
  - 17 tests: happy path (5), edge cases (7), error handling (5)
---

## 2026-02-25 - US-014
- Implemented personality trait system in personality/traits.py
- TraitProfile dataclass with 8 float dimensions (cynicism, wit, patience, curiosity, warmth, verbosity, formality, aggression)
- All values clamped to [0.0, 1.0] via __post_init__
- STAGE_DEFAULTS dict maps every CreatureStage to a hardcoded TraitProfile (matching TOML files)
- load_trait_profile() reads stage TOML via load_stage_config(), overlays on defaults, falls back if missing
- profile_from_config() creates TraitProfile from PersonalityConfig.base_traits
- to_dict() / from_dict() for serialization, from_dict ignores unknown keys
- Files changed: src/seaman_brain/personality/traits.py, tests/test_personality/test_traits.py
- **Learnings for future iterations:**
  - PersonalityConfig.base_traits is a dict[str, float] with all 8 trait keys — use TraitProfile.from_dict() to convert
  - Stage TOML [traits] section has exact trait names as keys — load_stage_config() returns StageConfig with .traits dict
  - Clamping via _clamp() in __post_init__ ensures no invalid values even from external TOML
  - load_trait_profile overlays TOML values onto STAGE_DEFAULTS (not bare defaults) so unspecified traits inherit stage-specific values
  - 26 tests: creation (6), stage defaults (6), clamping (4), TOML loading (4), config integration (2), edge cases (4)
---

## 2026-02-25 - US-015
- Implemented personality constraints and output filtering in personality/constraints.py
- FORBIDDEN_PHRASES: 30+ regex patterns for AI assistant cliches (case-insensitive)
- apply_constraints(response, profile) -> str: strips phrases, cleans whitespace, enforces length
- Verbosity-based length: linear interpolation from 10 chars (v=0.0) to 500 chars (v=1.0)
- _truncate() prefers sentence boundaries, then word boundaries, then hard truncate
- Files changed: src/seaman_brain/personality/constraints.py, tests/test_personality/test_constraints.py, prd.json
- **Learnings for future iterations:**
  - Regex patterns with `re.IGNORECASE` and `\b` word boundaries work well for phrase matching
  - Pre-compile patterns at module level for performance (list comprehension over FORBIDDEN_PHRASES)
  - After stripping phrases, clean up leading punctuation/whitespace artifacts (e.g. ", the water...")
  - Truncation should prefer sentence boundaries (". ") over word boundaries (" ") over hard cut
  - _max_length_for_verbosity uses linear interpolation: `MIN + (MAX - MIN) * verbosity`
  - 26 tests: phrase stripping (6), length enforcement (5), edge cases (9), helpers (6)
---

## 2026-02-25 - US-016
- Implemented PromptBuilder class in personality/prompt_builder.py
- Assembles system prompt from 7 sections: identity, stage speech, trait tone, mood/trust, needs hints, memories, negative constraints
- _STAGE_IDENTITIES and _STAGE_SPEECH dicts provide fallback descriptions per CreatureStage
- _NEGATIVE_CONSTRAINTS block with 11 strict rules preventing AI-assistant behavior
- _trait_tone_instructions() maps 8 trait dimensions to behavioral instructions via threshold buckets
- _mood_section() maps 8 named moods + 5 trust levels to prompt modifiers
- _memories_section() formats remembered facts as bulleted list with usage instruction
- _needs_hints() generates behavioral hints from hunger, health, interaction_count
- _get_stage_description() loads from TOML behavior.description, falls back to hardcoded
- creature_state is a dict[str, Any] to avoid dependency on unimplemented US-017 CreatureState
- Files changed: src/seaman_brain/personality/prompt_builder.py, tests/test_personality/test_prompt_builder.py, prd.json
- **Learnings for future iterations:**
  - PromptBuilder accepts creature_state as dict[str, Any] to decouple from US-017 CreatureState dataclass
  - Stage TOML behavior.description can override hardcoded _STAGE_IDENTITIES fallbacks
  - Mood section uses a dict mapping mood name -> prompt text; unknown moods fall back to raw name
  - Trust is bucketed into 5 levels (<0.2, <0.4, <0.6, <0.8, >=0.8) for graduated language
  - Trait tone uses threshold-based buckets (not linear interpolation) for clearer behavioral guidance
  - Negative constraints are the single most important section — they prevent the LLM from breaking character
  - 42 tests: basic build (5), with memories (4), different stages (4), creature state (8), trait tone (7), mood (6), stage desc (2), needs hints (6)
---

## 2026-02-25 - US-017
- Implemented CreatureState dataclass in creature/state.py with 11 fields (stage, age, interaction_count, mood, trust_level, hunger, health, comfort, last_fed, last_interaction, birth_time)
- All float fields clamped to [0.0, 1.0] via __post_init__, age and interaction_count clamped to non-negative
- to_dict() serializes to JSON-compatible dict (stage as .value, datetimes as ISO strings)
- from_dict() deserializes with graceful handling of missing/unknown keys
- Implemented StatePersistence class in creature/persistence.py with save/load/exists/delete
- save() creates directory if needed, backs up existing file to .json.bak before overwriting
- load() returns default CreatureState if file missing
- Files changed: src/seaman_brain/creature/state.py, src/seaman_brain/creature/persistence.py, tests/test_creature/test_state.py, tests/test_creature/test_persistence.py
- **Learnings for future iterations:**
  - CreatureState uses _clamp() helper in __post_init__ for float field validation — same pattern as TraitProfile
  - from_dict() uses datetime.fromisoformat() for timestamp deserialization — timestamps stored as ISO strings
  - StatePersistence uses shutil.copy2() for backup (preserves metadata)
  - data/saves/ is gitignored — tests must use tmp_path fixture for persistence tests
  - 46 tests: state creation (3), clamping (7), to_dict (3), from_dict (6), save/load (5), backup (3), missing file (4), errors (3), delete (2), all-stages roundtrip (5), helper (5)
---

## 2026-02-25 - US-018
- Implemented EvolutionEngine class in creature/evolution.py
- STAGE_ORDER tuple defines canonical progression: MUSHROOMER -> GILLMAN -> PODFISH -> TADMAN -> FROGMAN
- DEFAULT_THRESHOLDS dict provides fallback EvolutionThreshold for each non-first stage
- EvolutionEngine loads thresholds from CreatureConfig.evolution_thresholds (keyed by stage name), falls back to defaults
- check_evolution(state) evaluates only the immediate next stage — returns CreatureStage or None
- evolve(state, new_stage) validates the transition (no skip, no devolve, no same-stage), updates state.stage, returns TraitProfile
- Convenience: can_evolve() bool, stages_remaining() int
- Files changed: src/seaman_brain/creature/evolution.py, tests/test_creature/test_evolution.py
- **Learnings for future iterations:**
  - EvolutionEngine depends on CreatureConfig, CreatureState, and TraitProfile (via get_default_profile)
  - Config evolution_thresholds are keyed by stage name string (e.g. "gillman"), not enum — use CreatureStage(name.lower()) to convert
  - MUSHROOMER is the starting stage — no threshold defined for evolving *into* it
  - evolve() mutates CreatureState.stage in-place and returns the new TraitProfile
  - _stage_index() uses STAGE_ORDER.index() — all 5 stages must be in the tuple
  - 38 tests: stage order (3), default thresholds (3), engine init (6), check_evolution happy (5), check_evolution no-evolve (6), evolve happy (4), evolve errors (5), convenience (6)
---

## 2026-02-25 - US-019
- Implemented ContextAssembler class in conversation/context_assembler.py
- Assembles system prompt (SYSTEM), formatted retrieved memories (SYSTEM), and episodic messages in chronological order
- _estimate_tokens() helper uses ~4 chars/token heuristic for budget management
- Token budget trims oldest episodic messages first; memories skipped if they don't fit after system prompt
- _format_memories() creates bulleted list from MemoryRecord.text fields with instructional header
- Configurable max_tokens (default 4096), validated >= 1
- Files changed: src/seaman_brain/conversation/context_assembler.py, tests/test_conversation/test_context_assembler.py, prd.json
- **Learnings for future iterations:**
  - ContextAssembler is synchronous — no async needed since it just arranges data
  - Token estimation uses chars/4 heuristic — good enough for budget management without a tokenizer dependency
  - Input lists are copied before trimming (episodes = list(...)) to avoid mutating caller's data
  - Memory block is a SYSTEM message injected between the system prompt and episodic conversation
  - Budget trimming: system prompt always included, then memories if they fit, then episodic oldest-first trim
  - 27 tests: basic assembly (5), memory formatting (4), token budget (5), edge cases (6), errors (3), token estimation (4)
---

## 2026-02-25 - US-020
- Implemented ConversationManager class in conversation/manager.py — the central orchestrator
- Full conversation loop: add user msg to episodic -> update state (interaction count, trust, last_interaction) -> check evolution -> retrieve long-term memories -> build system prompt -> assemble context -> LLM chat -> apply personality constraints -> store assistant msg -> trigger extraction -> save state
- async initialize() sets up all subsystems from SeamanConfig with graceful degradation
- async shutdown() saves state and clears initialized flag
- Graceful degradation: LLM failure returns fallback response, memory retrieval/extraction failures logged and skipped, save failures logged
- get_state_summary() returns debug/display dict
- Constructor accepts optional pre-built LLM and CreatureState for testing
- Trust bump formula: `min(0.01, (1.0 - trust) * 0.02)` — diminishing returns
- Files changed: src/seaman_brain/conversation/manager.py, tests/test_conversation/test_manager.py, prd.json
- **Learnings for future iterations:**
  - ConversationManager accepts creature_state dict in PromptBuilder.build() via state.to_dict() — avoids coupling
  - Memory retrieval results are passed as text strings to PromptBuilder (not raw MemoryRecords to context_assembler)
  - Evolution check happens BEFORE LLM call so system prompt reflects new stage if evolved
  - The `from seaman_brain.types import ...` can be done inside process_input to avoid circular imports (though not needed here)
  - MockLLM for tests: just needs chat as AsyncMock — stream() not exercised by ConversationManager
  - Fallback response uses evolved flag to give stage-transition message on LLM failure
  - 35 tests: init (6), happy path (8), evolution (4), degradation (6), shutdown (3), summary (3), fallback (3), extraction (2)
---

## 2026-02-25 - US-021
- Implemented terminal CLI interface with rich output and prompt_toolkit input
- commands.py: 7 slash commands (/state, /memory, /stage, /traits, /reset, /quit, /help) with CommandResult dataclass and parse_command/execute_command functions
- terminal.py: SeamanTerminal class with stage/mood/trust header display, streaming-ready response processing, Ctrl+C/EOF graceful shutdown with state save
- Updated __main__.py to launch SeamanTerminal via asyncio.run when no --gui/--api flags
- Updated cli/__init__.py to export key classes
- Files changed: src/seaman_brain/cli/commands.py, src/seaman_brain/cli/terminal.py, src/seaman_brain/cli/__init__.py, src/seaman_brain/__main__.py, tests/test_cli/test_commands.py, tests/test_cli/test_terminal.py, prd.json
- **Learnings for future iterations:**
  - PromptSession() from prompt_toolkit fails in non-console environments (e.g. pytest on Windows bash) — must defer creation to first use (lazy init in _get_input)
  - ConversationManager is imported inside TYPE_CHECKING block + initialize() method in terminal.py — can't patch at module level, must patch at source (e.g. create_provider)
  - Signal handlers (SIGINT) may fail to install when not on main thread — wrap in try/except (ValueError, OSError)
  - _running flag must be set to False in finally block, not just via break — break exits loop but doesn't clear flag
  - MockLLM for CLI tests: same pattern as test_manager.py — AsyncMock for chat, async generator for stream
  - 54 tests: command parsing (7), help (2), state (2), memory (4), stage (2), traits (3), reset (2), quit (2), unknown (1), format_header (8), format_response (4), terminal init (4), display (3), run loop (6), shutdown (4)
---

## 2026-02-25 - US-022
- Implemented 21 integration tests for the brain core conversation loop in tests/test_integration/test_brain_core.py
- 5 test classes: TestEndToEndConversation (5 tests), TestMemoryExtraction (3 tests), TestEvolution (4 tests), TestSessionPersistence (4 tests), TestGracefulDegradation (5 tests)
- All tests use `pytestmark = pytest.mark.integration` — runnable with `pytest -m integration`
- Tests exercise full ConversationManager pipeline with mocked LLM at boundary only
- Files changed: tests/test_integration/__init__.py, tests/test_integration/test_brain_core.py, prd.json
- **Learnings for future iterations:**
  - PromptBuilder uses descriptive stage text, not stage enum names — check `_STAGE_IDENTITIES` for actual content
  - StatePersistence.save() is synchronous — use MagicMock not AsyncMock when mocking it
  - `pytestmark = pytest.mark.integration` at module level applies marker to all tests in the module
  - MockLLM with response queue pattern: `side_effect=self._next_response` on AsyncMock allows multi-turn tests with different responses
  - LanceDB `db_path` must be set in MemoryConfig when testing extraction that stores to semantic memory
  - 21 tests: end-to-end (5), extraction (3), evolution (4), persistence (4), degradation (5)
---

## 2026-02-25 - US-023
- Implemented TTSProvider Protocol (runtime-checkable) with async synthesize(text) -> bytes and speak(text) -> None
- Pyttsx3TTSProvider: offline TTS using pyttsx3, runs in ThreadPoolExecutor to avoid blocking event loop
- NoopTTSProvider: silent fallback returning valid empty WAV bytes
- create_tts_provider() factory with graceful fallback to NoopTTSProvider on failure
- Configurable voice, rate, volume from AudioConfig; volume clamped to [0.0, 1.0]
- synthesize() saves to temp WAV file, reads back bytes; speak() uses engine.say() + runAndWait()
- Updated audio/__init__.py to export TTSProvider, Pyttsx3TTSProvider, NoopTTSProvider, create_tts_provider
- Files changed: src/seaman_brain/audio/tts.py, src/seaman_brain/audio/__init__.py, tests/test_audio/test_tts.py, prd.json
- **Learnings for future iterations:**
  - pyttsx3 is imported inside methods (not at module level) — mock via `patch.dict(sys.modules, {"pyttsx3": mock_mod})`, NOT `patch("seaman_brain.audio.tts.pyttsx3")`
  - Setting sys.modules["pyttsx3"] = None triggers ImportError on `import pyttsx3` — useful for testing missing deps
  - pyttsx3 engines are NOT thread-safe — use single-thread ThreadPoolExecutor for all operations
  - wave module's `wave.open(buf, "wb")` with BytesIO works for generating valid WAV headers in-memory
  - pyttsx3.init() must be called fresh per-operation (no reuse) since engines don't survive cross-thread
  - save_to_file() + runAndWait() is the pyttsx3 pattern for getting audio bytes (no direct API)
  - 35 tests: protocol (4), noop (4), init (4), engine config (6), synthesize (4), speak (4), factory (6), edge cases (3)
---
